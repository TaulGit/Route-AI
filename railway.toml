[build]
builder = "nixpacks"
build_command = "pip install -r backend/requirements.txt"

[deploy]
start_command = "cd backend && uvicorn app.api.main:app --host 0.0.0.0 --port ${PORT:-8000} --workers 1"
healthcheck_path = "/health"
